#include <stdio.h>
#include <stdlib.h>
int main()
{
    int a =100 , b = 25;
    printf("inicio la app \n");
       //Estructura condicional simple
    if ( a > b ) {
            //cuerpo del if,(sentencia/s a ejecutar si la condicio de cumple)
        printf("A es mayor a B\n");
    }

    printf("Fin de la app\n");
    return 0;
}
